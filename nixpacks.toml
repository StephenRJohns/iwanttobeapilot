[variables]
NPM_CONFIG_INCLUDE = "optional"

[phases.install]
cmds = ["npm ci --ignore-scripts --include=optional"]

[phases.build]
cmds = ["bash scripts/railway-build.sh"]

[start]
cmd = "npm start"
